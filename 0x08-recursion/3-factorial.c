#include "main.h"

/**
 * factorial(int n) - a funtion to return the factorial of a number
 * @h: the integer to be factorialized
 * Return - returns -1 in case of error
 */
int factorial(int h)
{
	if (h < )
		return(-1);
	if (h == 1)
		return (1);
	return (n * factorial(h - 1);
}
