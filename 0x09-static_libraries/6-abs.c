#include "main.h"

/**
 * _abs - a funtion
 * @n: an input
 * Description: This function returns absolute value of a number
 * Return: returns an int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
