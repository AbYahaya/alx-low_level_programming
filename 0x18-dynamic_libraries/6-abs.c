#include "main.h"

/**
 * _abs - checks code
 * @r: an int
 * Description:returns absolute value
 * Return: Absolute value of number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
